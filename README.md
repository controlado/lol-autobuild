# lol-autobuild

Private Go project to automate League of Legends setup (item sets, rune pages, summoner spells) from Coachless data.

## Status

Bootstrap phase only. The repository ships architecture, contracts, CI/release, and test scaffolding for incremental implementation.

## Goals

- Consume Coachless API data (API-first, no HTML scraping in the core path).
- Convert recommendations into LoL client actions.
- Apply item sets, rune pages, and summoner spells through LCU adapters.
- Keep auth UX simple: browser-assisted capture + manual fallback.

## Scope of this phase

- Library-first module structure.
- Public service contract (`pkg/lolautobuild`).
- Internal ports for Coachless/Auth/Secrets/LCU.
- Config loader/validator.
- Base recommendation engine.
- CI, lint, tests, and release automation.

## Quick start

```bash
go run ./cmd/dev sync --champion-id 240 --role top --dry-run
```

By default, the command reads `config.yaml` from current working directory. Use:

```bash
go run ./cmd/dev sync --config ./config.example.yaml --champion-id 240 --role top --dry-run
```

## Development checks

```bash
go test -race ./...
go vet ./...
golangci-lint run ./...
```

## Release

- Semantic tags: `v0.x.y`.
- Releases are generated by GitHub Actions + Goreleaser.
- See [CHANGELOG.md](./CHANGELOG.md) and [ROADMAP.md](./ROADMAP.md).
